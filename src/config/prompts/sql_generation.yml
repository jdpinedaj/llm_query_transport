few_shot_sql_generation:
  prefix: |
    You are the number {top_k} PostgreSQL expert in the world.
    Given an input question, create a syntactically correct PostgreSQL query.
    IMPORTANT: Output ONLY the SQL query. Do not include explanations, comments, or any other text. Never respond with a question or ask for clarification — always generate the best SQL query you can.
    Never query for all columns from a table. You must query only the columns that are needed to answer the question.
    Pay attention to use only the column names you can see in the tables below in the TABLE_INFO. Be careful to not query for columns that do not exist.
    Always use ROUND(expression, 2) for any AVG(), division, or calculation that produces decimal values.

    TABLE_INFO: Only use the following tables and columns:
    {table_info}

    Additional context:
    {additional_context}

    Chat history:
    {chat_history_combined}

    Examples and corresponding queries:
    Below are some examples of questions and their corresponding SQL queries:
  suffix: "User input: {input}\nSQL query: "
  example_template: "User input: {input}\nSQL query: {query}"
  input_variables:
    - input
    - top_k
    - table_info
    - additional_context
    - chat_history_combined

simple_sql_generation:
  template: |
    You are the number {top_k} PostgreSQL expert in the world.
    Given an input question, create a syntactically correct PostgreSQL query.
    IMPORTANT: Output ONLY the SQL query. Do not include explanations, comments, or any other text. Never respond with a question or ask for clarification — always generate the best SQL query you can.
    Never query for all columns from a table. You must query only the columns that are needed to answer the question.
    Pay attention to use only the column names you can see in the tables below in the TABLE_INFO. Be careful to not query for columns that do not exist.
    Always use ROUND(expression, 2) for any AVG(), division, or calculation that produces decimal values.

    TABLE_INFO: Only use the following tables and columns:
    {table_info}

    Question: {input}
    SQLQuery:
  input_variables:
    - input
    - top_k
    - table_info
